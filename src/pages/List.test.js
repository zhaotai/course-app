import List from './List';
import ShallowRenderer from 'react-test-renderer/shallow';

describe('list test', () => {
  it('shallow render', () => {
    // const renderer = new ShallowRenderer();
    // renderer.render(<List />);
    // expect(List.prototype._getList([{id: ""}])).toMatchSnapshot();
  })
});